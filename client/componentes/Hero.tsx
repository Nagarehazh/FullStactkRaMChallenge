import Image from "next/image";

const Hero = () => {
    return (
        <div className="bg-white h-screen flex items-center justify-center">
           Hero
        </div>
    );
}

export default Hero;